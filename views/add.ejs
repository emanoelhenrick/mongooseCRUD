<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Encurtadort</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
</head>
<body>

    <div class="container w-50">
        <h1>Add Link</h1>
        <form class="d-flex flex-column" action="/" method="POST">
            <input class="mb-2" name="title" type="text" placeholder="titulo" value="<%= body.title %>" required>
            <input class="mb-2" name="description" type="text" placeholder="description" value="<%= body.description %>" required>
            <input class="mb-4" name="url" type="text" placeholder="url" value="<%= body.url %>" required>
            <button class="btn btn-primary">Salvar</button>
        </form>
        <% if(error){ %>
            <div class="alert alert-danger mt-3">
               <%= error.message %>
            </div>
         <% } %>
    </div>

    
</body>
</html>